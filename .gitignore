# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
userhub.db

# 部署引擎工作目录
installer/workspace/

# uv 相关(uv.lock 应提交,勿忽略)
# uv.lock

# 构建产物
dist/
*.egg-info/
installer/app/static/

# Node
node_modules/
dist/

# 部署引擎工作目录
backend/workspace/

# 系统文件
.DS_Store
nohup.out
# 编辑器残留(vim 交换文件/备份): 曾差点被 build-cli-context 的 rsync 一起打进分发镜像
*.swp
*.swo
*.swx
*~

# 集群统一配置(含密码,勿提交;模板 cluster.conf.example 需提交)
deployments/config/cluster.conf
deployments/config/cluster.conf.bak
deployments/config/cluster.conf.bak.*
# 外部 Ceph 官方接入 env(含真实 CSI key/RGW admin 密钥, 勿提交; 由提供方导出)
deployments/config/external-ceph.env
# 外部 Ceph 自研导出配置(**由 ceph-expose-external.sh 生成, 含真实 keyring**, 勿提交)
# ⚠ 2026-09-20 事故: 该文件被生成器从"示例"覆写成真值后仍处于**被跟踪**状态, 险些随提交入库
deployments/config/external-ceph-self-define-access.conf
# MinIO 独立配置(含密钥,勿提交;模板 minio.conf.example 需提交, 供 fetch-offline-from-minio.sh 使用)
deployments/config/minio.conf
# Docker CLI 镜像独立构建上下文(生成目录, build-cli-context.sh 每次构建前重新生成)
deployments/cli-context/
# 断点状态文件(含进度,勿提交)
deployments/config/.deploy.state
deployments/config/.deploy.state.lock
# kubespray 生成的 artifacts(kubeconfig/kubectl,含凭据, root 属主)
deployments/kubespray/inventory/*/artifacts/
# 部署生成的 inventory(含密码)
deployments/kubespray/inventory/cubestack-cluster/hosts.yml
deployments/kubespray/inventory/cubestack-cluster/inventory.ini
# 磁盘链路回归测试的夹具(含**生产集群标识**: IP 10.66.3.28/29/31、主机名 mxgpu-3-*、真实 Ceph fsid
# 与生产节点 lsblk 布局)。测试脚本与夹具是一套、缺夹具跑不起来, 故整个目录暂不入库;
# ★ 待脱敏(IP/主机名/fsid 换成中性值并重跑 41 项断言)后**删掉本行**再提交。
# ⚠ 注意: `check-modules.sh` 第 ⑫ 项在此目录缺席时会打印"跳过(未找到)" —— 这是设计好的降级, 不是错误。
deployments/scripts/tools/tests/

# 虚拟机基础镜像/磁盘(大文件,勿提交)
deployments/virtual-machine/

# 离线文件(镜像 tar / 二进制 / .deb / 资源包, 大文件不入库; 路径由 OFFLINE_FILES_DIR 等变量切换)
#   ⚠ 这里分四行写而不是直接忽略整个目录: 需要放行**各组件子目录下的 README.md**
#     (说明该目录放什么、怎么备料), 其余内容(镜像 tar / 二进制 / .deb)仍全部忽略。
#     顺序要紧(git 后匹配者优先):
#       ① 忽略 offline-files 下所有内容
#       ② 放行一级子目录本身(否则 git 不往里走, 后面的 ! 无从生效)
#       ③ 重新忽略二级内容(把 ② 放行进来的 tar/二进制挡回去; 三级及以下随目录一起被忽略)
#       ④ 只把二级的 README.md 放行
#     若改回单行 `deployments/offline-files/`, README 会进不了 git;
#     若只写 ①②, 离线 tar 会被误纳入版本控制(2026-09-18 踩过)。
deployments/offline-files/*
!deployments/offline-files/*/
deployments/offline-files/*/*
!deployments/offline-files/*/README.md
deployments/kubespray/kubespray/.git
deployments/kubespray/kubespray/.gitattributes
deployments/kubespray/kubespray/.github
deployments/kubespray/kubespray/.gitignore
deployments/kubespray/kubespray/.gitlab-ci
deployments/kubespray/kubespray/.gitlab-ci.yml
deployments/kubespray/kubespray/.gitmodules

# 部署引擎在宿主机生成的临时拷贝(按 ssh 目标命名)
/deployments/*@*/
cluster.conf.backup
